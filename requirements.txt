# CoreAI – dependency manifest
# Install with:
#   pip install -r requirements.txt
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Deep learning (CNN on MNIST + built-in CoreGPT LLM)
torch>=2.2.0
torchvision>=0.17.0

# Classical ML
scikit-learn>=1.4.0
joblib>=1.3.2

# Numerics
numpy>=1.26.0
pandas>=2.2.0

# System monitoring
psutil>=5.9.8

# Terminal UI
rich>=13.7.0

# Web server (CoreAI web UI)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Web search (DuckDuckGo, no API key required)
duckduckgo-search>=5.3.0

# NOTE: no external LLM API needed – the language model is built and trained
# from scratch inside coreai.py using only PyTorch.
pypdf
python-multipart
